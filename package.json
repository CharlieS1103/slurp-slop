{
  "name": "slurpslop",
  "version": "2.1.0",
  "type": "module",
  "description": "Professional search results by slurping up AI Overview slop, low-quality sites, and advertisements from Google Search",
  "scripts": {
    "build": "npm run clean && npm run copy && npm run generate:icons && npm run minify && npm run obfuscate",
    "build:dev": "npm run clean && npm run copy && npm run generate:icons",
    "clean": "rm -rf dist/",
    "copy": "npm run copy:static && npm run copy:popup && npm run copy:js && npm run copy:css",
    "copy:static": "mkdir -p dist && cp manifest.json rules.json rules-gemini.json rules-chatgpt.json rules-claude.json rules-grok.json rules-deepseek.json src/popup/popup.html dist/ && cp -r icons dist/",
    "copy:popup": "mkdir -p dist/popup/images && cp -r src/popup/images/* dist/popup/images/ || true",
    "copy:js": "mkdir -p dist/js && cp src/popup/*.js dist/js/ && node scripts/bundle-content.mjs && cp src/content/features/background.js dist/background.js",
    "copy:css": "mkdir -p dist/css && cp src/css/*.css dist/css/",
    "generate:icons": "node scripts/generate-icons.mjs",
    "minify": "npm run minify:js && npm run minify:css",
    "minify:js": "npx terser dist/js/popup.js -o dist/js/popup.js --compress --mangle && npx terser dist/js/utils.js -o dist/js/utils.js --compress --mangle && npx terser dist/content-script.js -o dist/content-script.js --compress --mangle",
    "minify:css": "npx cleancss -o dist/css/popup.css dist/css/popup.css",
    "obfuscate": "npm run obfuscate:js",
    "obfuscate:js": "npx javascript-obfuscator dist/js/popup.js --output dist/js/popup.js --compact true --control-flow-flattening true --dead-code-injection true --string-array true --string-array-encoding base64 --transform-object-keys true && npx javascript-obfuscator dist/js/utils.js --output dist/js/utils.js --compact true --control-flow-flattening true --dead-code-injection true --string-array true --string-array-encoding base64 --transform-object-keys true && npx javascript-obfuscator dist/content-script.js --output dist/content-script.js --compact true --control-flow-flattening true --dead-code-injection true --string-array true --string-array-encoding base64 --transform-object-keys true",
    "lint": "npx eslint \"src/**/*.js\" --max-warnings=0",
    "lint:fix": "npx eslint \"src/**/*.js\" --fix",
    "format": "npx prettier --write \"src/**/*.{js,css,html,json,md}\" \"*.{md,json}\" manifest.json",
    "package": "npm run build && npm run zip",
    "zip": "cd dist && zip -r ../slurpslop-v$(node -p \"require('../package.json').version\").zip . && cd ..",
    "precommit": "npm run format && npm run lint:fix && npm run generate:icons"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "clean-css-cli": "^5.6.3",
    "esbuild": "^0.21.5",
    "eslint": "^9.0.0",
    "javascript-obfuscator": "^4.0.2",
    "nodemon": "^3.0.3",
    "prettier": "^3.2.5",
    "sharp": "^0.33.5",
    "terser": "^5.31.0",
    "webpack-bundle-analyzer": "^4.10.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/CharlieS1103/slurp-slop.git"
  },
  "author": "CharlieS1103",
  "license": "MINEMINEMINE",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "dependencies": {
    "esbuild-wasm": "^0.25.12"
  }
}
